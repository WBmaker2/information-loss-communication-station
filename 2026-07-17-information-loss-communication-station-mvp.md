# 정보 손실 통신소 MVP 구현 계획

**목표:** 초등 3~6학년 학생이 하나의 원문 메시지가 요약·전달·재전달되는 과정에서 어떻게 달라지는지 단계별로 비교하고, 빠진 정보·근거 없이 더해진 정보·뜻이 달라진 표현·뜻을 유지한 바꾸어 쓰기를 구분한 뒤 핵심 의미를 보존한 전달문을 다시 구성하는 서버 없는 국어 학습 웹앱을 구현합니다.

**핵심 학습 경험:** 학생은 마지막 메시지만 보고 원문과 다른 낱말을 모두 오류로 표시하지 않습니다. 먼저 원문의 의미를 `누가·무엇을·언제·어디서·어떤 조건으로·얼마나 확실하게`라는 정보 조각으로 확인합니다. 이어서 원문과 1차 전달문, 1차 전달문과 재전달문을 차례로 비교하여 각 변화가 정보 생략인지, 새 정보 추가인지, 의미 변경인지, 뜻을 유지한 표현 변경인지 판정합니다. 마지막에는 꼭 전달해야 할 정보와 불확실성·조건 표현을 보존하여 여러 가지 가능한 안전한 전달문 중 하나를 완성합니다.

**권장 아키텍처:** 향후 별도 저장소 `information-loss-communication-station`에 Vite, React, TypeScript 기반 정적 앱을 구성합니다. 사람이 검수한 가상 메시지 사건을 제공하는 `TransmissionCaseCatalog`, 각 단계의 의미 단위와 표현 조각을 연결하는 `MeaningLedger`, 단계 사이 변화 유형을 집합으로 판정하는 `ChangeJudge`, 필수 정보 보존과 근거 없는 추가를 검사하는 `SafeRelayValidator`, 학습 흐름 상태를 관리하는 리듀서를 분리합니다. 자연어 처리, 생성형 AI, 자유 서술 자동 채점, 실제 메신저 가져오기, 서버, 로그인, 외부 API, 브라우저 영구 저장은 사용하지 않습니다.

**기술 스택:** Vite, React, TypeScript, CSS, SVG, Vitest, React Testing Library, Playwright, `@axe-core/playwright`, Node.js

**현재 상태:** 이 문서는 구현 계획만 정의합니다. 별도 구현 요청 전에는 새 저장소 생성, 패키지 설치, 앱 코드·삽화·메시지 원고 제작, 테스트 실행, 커밋, 푸시, 배포, HVC 등록, 공개 갤러리 동기화를 하지 않습니다.

## 1. 확인 자료와 설계 전제

### 확인 자료

- 아이디어 출처: [`docs/2026-07-17-next-education-webapp-ideas.md`](../../2026-07-17-next-education-webapp-ideas.md)
- 기존 앱 비교: [`docs/vibehong-webapp-catalog.md`](../../vibehong-webapp-catalog.md), 2026-07-17 문서 기준 71개 주제
- 직접 비교 앱: `팩트체크 편집국`, `AI 데이터 품질검사소`
- 인접 비교 앱: `지시어 관제실`, `뚝딱뚝딱 문장 만들기 공장`, `실험 리포트 조립소`, `LinkEffect: 인과관계 이어달리기`, `이야기 마음 근거 수사대`
- 성취기준 원문: `★(초)2022개정교육과정에따른성취수준(3~4학년군).pdf`, `★(초)2022개정교육과정에따른성취수준(5~6학년군).pdf`
- `[4국01-01]`은 3~4학년군 PDF 뷰어 21쪽, 문서 내부 인쇄 17쪽에서 확인했습니다.
- `[4국04-05]`는 3~4학년군 PDF 뷰어 25쪽, 문서 내부 인쇄 21쪽에서 확인했습니다.
- `[6국01-01]`은 5~6학년군 PDF 뷰어 21쪽, 문서 내부 인쇄 17쪽에서 확인했습니다.
- `[6국02-02]`는 5~6학년군 PDF 뷰어 23쪽, 문서 내부 인쇄 19쪽에서 확인했습니다.
- `[6국03-04]`는 5~6학년군 PDF 뷰어 24쪽, 문서 내부 인쇄 20쪽에서 확인했습니다.

### 기존 대화에서 이어받는 원칙

- 기존 앱과 소재만 다르고 핵심 화면·판정 흐름이 같은 앱을 만들지 않습니다.
- 외부 자료의 진실성이나 출처 신뢰도를 평가하는 팩트체크 앱으로 만들지 않습니다.
- 데이터 표의 누락·중복·형식 오류를 정비하는 데이터 품질 앱으로 만들지 않습니다.
- 원문과 최종문만 비교하지 않고 `원문 → 1차 전달 → 재전달`의 인접 단계를 차례로 비교합니다.
- 원문과 다른 낱말을 모두 오류로 보지 않고 뜻을 유지한 바꾸어 쓰기를 정상적인 전달로 인정합니다.
- 짧은 문장이 항상 좋은 요약이라고 가르치지 않고 전달 목적에 필요한 정보 보존을 우선합니다.
- 학생에게 소문을 만들거나 실제 친구 이야기를 입력·전달하게 하지 않습니다.
- 특정 학생, 학교 사건, 가족사, 건강 정보, 갈등, 괴롭힘을 학습 소재로 사용하지 않습니다.
- 드래그가 없어도 터치·키보드·스위치 입력으로 모든 활동을 완주할 수 있어야 합니다.
- 점수·속도보다 변화 위치 찾기, 의미 보존 판단, 근거 설명, 안전한 재전달을 중심으로 합니다.
- 실제 구현 시 화면의 적절한 위치에 작은 `업데이트 내역` 버튼을 두고 개발일과 개선일을 최신순으로 기록합니다.
- 모든 코드 파일은 500줄 미만으로 유지하고 기능별로 분리합니다.

## 2. 교육과정 연결

### 직접·부분 연결 표

| 역할 | 성취기준·성취수준 | 앱에서 지원하는 활동 | 연결 한계 | 원문 위치 |
|---|---|---|---|---|
| 3~4학년 핵심·부분 연결 | `[4국01-01] 중요한 내용과 주제를 파악하며 듣고 그 내용을 요약한다.` | 짧은 원문에서 꼭 전달해야 할 중심 정보와 보충 정보를 구분하고, 요약문에 남은 정보와 빠진 정보를 확인합니다. | 실제 음성 듣기와 학생 자신의 언어로 자유롭게 요약하는 능력 전체를 평가하지 않습니다. MVP는 화면에 제시된 검수 문장과 의미 블록을 사용합니다. | 3~4학년군 PDF 21쪽 |
| 태도 확장 | `[4국04-05] 언어가 의사소통과 관계 형성의 수단임을 이해하고 국어를 소중히 여기는 태도를 지닌다.` | 전달 과정에서 작은 표현 변화가 상대의 이해에 영향을 줄 수 있음을 확인하고, 정확하고 배려 있게 다시 전달하는 이유를 돌아봅니다. | 국어를 소중히 여기는 태도 전체를 앱 완료 여부로 측정하지 않습니다. | 3~4학년군 PDF 25쪽 |
| 5~6학년 핵심 연결 | `[6국01-01] 대화에서 생략된 내용을 추론하며 듣는다.` | 전달문에서 생략된 시간·장소·대상·조건을 이전 단계와 맥락에 근거하여 찾아냅니다. | 실제 음성 대화의 억양·표정·즉시 상호작용은 다루지 않습니다. | 5~6학년군 PDF 21쪽 |
| 읽기 확장 | `[6국02-02] 글에서 생략된 내용이나 함축된 표현을 문맥을 고려하여 추론한다.` | 각 전달 단계의 앞뒤 문맥을 비교하여 생략된 정보와 뜻이 달라진 표현을 추론합니다. | 비유·반어·풍자·복잡한 문학적 함축은 다루지 않고 정보 전달 메시지로 범위를 제한합니다. | 5~6학년군 PDF 23쪽 |
| 쓰기 부분 연결 | `[6국03-04] 독자와 매체를 고려하여 내용을 생성하고 표현하며 글을 쓴다.` | 교실 안내, 모둠 메모, 게시판 공지처럼 전달 대상과 매체가 정해진 상황에서 필수 정보를 보존한 전달문을 구성합니다. | 자유 글쓰기와 여러 매체의 제작 기능 전체를 평가하지 않습니다. 검수된 의미·표현 블록으로 구성합니다. | 5~6학년군 PDF 24쪽 |

### 성취수준 적용 원칙

- 3~4학년 기본 항로는 `[4국01-01]`의 중심 내용과 세부 내용 구분을 `꼭 전달할 정보`와 `있으면 도움이 되는 정보`로 바꾸어 제시합니다.
- 3~4학년은 두 단계, 3~4개의 의미 단위, `빠짐·더해짐·달라짐·뜻 유지`의 쉬운 분류를 사용합니다.
- 5~6학년 확장 항로는 `[6국01-01]`, `[6국02-02]`의 생략 추론을 세 단계 전달 기록에 적용합니다.
- 5~6학년은 조건, 부정, 수량 범위, 불확실성, 출처처럼 빠지거나 바뀌면 의미가 크게 달라지는 요소를 추가합니다.
- `[6국03-04]`는 전달 대상과 매체를 확인한 재작성 활동으로 일부 지원하되 자유 글쓰기 전체를 달성했다고 표시하지 않습니다.
- C 수준 학생은 한 번에 한 변화만 비교하고 의미 조각을 색·아이콘·문장으로 함께 볼 수 있게 합니다.
- A 수준 학생에게는 둘 이상의 타당한 안전 전달문을 비교하고 더 간결한 이유를 설명하는 확장 질문을 제공합니다.
- 학생 화면에 A·B·C 등급, 점수, 백분율, 순위, 제한 시간을 표시하지 않습니다.
- 교사용 관찰 항목은 `중심 정보`, `단계 비교`, `변화 유형`, `근거`, `조건·불확실성 보존`, `안전한 재전달`로 제한합니다.

## 3. 국어 개념과 판정 계약

### 학생용·교사용 용어

| 구분 | 학생 화면 표현 | 교사용 설명 | 피할 오해 |
|---|---|---|---|
| 원문 | 처음 메시지 | 전달 사슬의 기준이 되는 검수된 최초 메시지 | 무조건 가장 긴 문장 |
| 의미 조각 | 꼭 확인할 정보 조각 | 사람·행동·시간·장소·수량·조건·부정·확실성·출처 등 독립적으로 추적할 명제 단위 | 낱말 하나와 항상 같음 |
| 빠진 정보 | 전달하며 빠졌어요 | 이전 단계에 있었지만 다음 단계에서 표현되거나 문맥상 복원되지 않는 의미 조각 | 문장이 짧아진 모든 경우 |
| 새로 더한 정보 | 근거 없이 더했어요 | 이전 단계에 없고 원문이나 허용 문맥으로 뒷받침되지 않는 새 의미 | 자세히 설명한 모든 경우 |
| 뜻이 달라진 표현 | 말은 남았지만 뜻이 바뀌었어요 | 대상·행동·시간·장소·수량·조건·부정·확실성 중 하나 이상이 이전 단계와 달라진 표현 | 다른 낱말을 쓴 모든 경우 |
| 뜻을 지킨 바꾸어 쓰기 | 표현은 달라도 뜻은 같아요 | 핵심 의미와 범위·조건·확실성이 보존된 검수된 바꾸어 쓰기 | 원문과 글자가 같아야 정답 |
| 안전한 전달문 | 꼭 필요한 뜻을 지킨 전달문 | 목적에 필요한 필수 의미를 보존하고 근거 없는 내용을 넣지 않으며 불확실성·조건을 숨기지 않은 문장 | 무조건 길고 자세한 문장 |

### 변화 유형의 고정 정의

| 내부 코드 | 판정 조건 | 대표 예 | 판정하지 않을 예 |
|---|---|---|---|
| `omission` | 이전 단계의 의미 조각이 다음 단계에 없고 문맥으로도 복원할 수 없음 | `비가 오면 체육관`에서 조건이 빠져 `체육관`만 전달됨 | `오후 2시`를 `14시`로 바꿈 |
| `unsupported-addition` | 이전 단계와 허용 문맥에 없는 새 의미가 다음 단계에 생김 | `준비물을 가져오세요`가 `반드시 새것을 사 오세요`가 됨 | `준비물` 뒤에 원문의 목록을 풀어 씀 |
| `meaning-shift` | 대응 표현이 남아 있으나 의미 속성이 달라짐 | `2시 이후`가 `2시`로, `두 명까지`가 `두 명`으로 바뀜 | `모둠별 한 장`을 `각 모둠은 한 장`으로 바꿈 |
| `meaning-preserving` | 표면 표현은 달라도 검수된 의미 조각 집합과 범위가 같음 | `운동장에 모여요`를 `모이는 곳은 운동장이에요`로 바꿈 | 조건이나 부정을 빼고 핵심만 비슷하게 남김 |
| `unchanged` | 표현과 의미가 모두 실질적으로 유지됨 | 같은 문구가 그대로 전달됨 | 띄어쓰기만 다른 것을 별도 학습 오류로 채점함 |

### 고정 의미 보존 원칙

- 전달 단계는 바로 이전 단계와 먼저 비교하고, 사건 마지막에 전체 사슬을 원문과 다시 대조합니다.
- 요약은 덜 중요한 표현을 줄일 수 있지만 전달 목적에 필요한 의미 조각을 없애면 안전한 요약으로 보지 않습니다.
- 낱말이 달라도 대상·행동·시간·장소·수량·조건·부정·확실성이 같으면 의미 보존 후보가 될 수 있습니다.
- 같은 낱말이 남아 있어도 `전까지·이후`, `가능·확정`, `일부·모두`, `하지 않음·해도 됨`이 달라지면 의미 변경입니다.
- `아마`, `예정`, `확인 중`, `선생님이 알림`처럼 확실성과 출처를 나타내는 정보는 임의로 확정 표현으로 바꾸지 않습니다.
- 이전 단계에 없는 이유·감정·평가·추측을 새로 넣으면 자연스러워 보여도 근거 없는 추가입니다.
- 조사·어미·문장 순서·동의 표현의 차이만으로 뜻이 달라졌다고 판정하지 않습니다.
- 여러 개의 자연스러운 바꾸어 쓰기가 가능하므로 안전한 전달문은 하나의 문자열 정답으로 채점하지 않습니다.
- 메시지가 짧아졌는지보다 필수 의미가 보존되었는지를 먼저 확인합니다.
- 앱의 판정은 검수된 가상 사건 안에서만 유효하며 실제 사람의 의도나 책임을 판정하지 않습니다.

### 고정 안내 문구

- `마지막 문장만 보지 말고 바로 앞 전달 단계와 비교해요.`
- `표현이 달라도 뜻이 같으면 정보 손실이 아니에요.`
- `짧게 줄여도 꼭 필요한 시간·장소·조건은 남겨야 해요.`
- `원문에 없는 내용을 그럴듯하게 더하지 않아요.`
- `“아마”를 “확실히”로 바꾸면 확실한 정도가 달라져요.`
- `실제 친구 이야기는 쓰지 않고 준비된 가상 메시지만 살펴봐요.`

## 4. 제품 접근 비교와 결정

| 접근 | 방식 | 장점 | 위험 | 결정 |
|---|---|---|---|---|
| 검수된 의미 이력 추적 | 고정 원문과 2~3단계 전달문에서 의미 조각과 변화 유형을 비교하고 블록으로 재작성 | 판정이 결정적이고 복수의 바꾸어 쓰기를 인정하며 서버 없이 동작 | 콘텐츠 의미 주석과 검수 부담이 큼 | **MVP 채택** |
| 귓속말 전달 게임 | 학생들이 실제 문장을 말로 이어 전달하고 마지막 결과 비교 | 재미와 참여도가 높음 | 친구의 실수 조롱, 실제 소문, 청각·말하기 접근성, 수업 통제 문제가 생길 수 있음 | 앱 핵심에서 제외 |
| 자유 문장 차이 찾기 | 학생이 두 문장을 입력하면 자동으로 달라진 부분을 표시 | 다양한 문장을 다룰 수 있음 | 글자 차이와 의미 차이를 혼동하고 자연어 처리·개인정보 범위가 커짐 | 제외 |
| 생성형 AI 요약·판정 | 자유 원문을 요약하고 손실을 설명 | 콘텐츠 확장성이 큼 | 결과 변동, 근거 불투명, 비용, 네트워크, 부적절 입력 문제가 있음 | 제외 |
| 원문과 최종문만 비교 | 처음과 끝의 다른 부분만 찾음 | 화면이 단순함 | 어느 단계에서 변화가 생겼는지 알 수 없고 전달 과정 학습이 사라짐 | 사용하지 않음 |

### 채택안의 핵심 이유

- 전달 사슬을 인접 단계별로 비교하여 정보가 처음 바뀐 지점을 찾을 수 있습니다.
- 글자 차이와 의미 차이를 분리하여 동의 표현·바꾸어 쓰기를 오류로 과잉 판정하지 않습니다.
- 조건·부정·수량·확실성처럼 작아 보여도 중요한 표현을 의미 단위로 추적할 수 있습니다.
- 검수된 의미 조각 집합으로 복수의 안전 전달문을 결정적으로 인정할 수 있습니다.
- 실제 학생 메시지와 자유 입력을 사용하지 않아 개인정보와 관계 갈등 위험을 줄입니다.

## 5. 기존 앱과의 차별화

| 기존 앱·계획 | 겹칠 수 있는 요소 | 정보 손실 통신소의 차별화 기준 |
|---|---|---|
| 팩트체크 편집국 | 주장, 근거, 판정 카드 | 외부 자료로 주장 사실성을 판정하지 않습니다. 하나의 검수된 원문이 전달 단계마다 어떻게 변했는지 내부 이력만 추적합니다. |
| AI 데이터 품질검사소 | 누락·오류·수정 분류 | 표·레코드·라벨·중복·대표성을 정비하지 않습니다. 자연어 메시지의 의미 조각과 조건·확실성 변화를 비교합니다. |
| 지시어 관제실 | 짧은 문맥, 모호함, 문장 명료화 | `이것·그곳·그 사람`의 지시 대상을 찾지 않습니다. 대상이 분명한 원문도 재전달에서 정보가 빠지거나 더해지는 전체 의미 변화를 추적합니다. |
| 뚝딱뚝딱 문장 만들기 공장 | 문장 블록과 완성 문장 | 낱말 어순, 문장 짜임, 문장 부호를 맞추지 않습니다. 완성 순서보다 필수 의미 보존과 근거 없는 추가 방지가 판정 기준입니다. |
| 실험 리포트 조립소 | 사실·해석 구분, 문장 조립 | 관찰 사실과 과학적 해석을 구분하거나 실험 보고서를 만들지 않습니다. 동일 메시지의 전달 전후 의미 보존만 다룹니다. |
| LinkEffect: 인과관계 이어달리기 | 단계 연결, 화살표, 문장 카드 | 사건의 원인과 결과를 논리적으로 잇지 않습니다. 시간 순서가 정해진 전달 단계 사이에서 의미가 처음 변한 지점을 표시합니다. |
| 이야기 마음 근거 수사대 | 이야기 읽기와 근거 표시 | 인물의 마음이나 행동 이유를 추론하지 않습니다. 정보 전달문에 명시된 의미와 생략·추가·변경만 판정합니다. |

### 차별화 검수 규칙

- 핵심 순환은 `원문 뜻 펼치기 → 인접 단계 비교 → 변화 조각 표시 → 변화 유형 판정 → 전체 사슬 점검 → 안전 전달문 구성 → 보존 기록 확인`입니다.
- 외부 검색, 출처 신뢰도 등급, 참·거짓 판정, 데이터 레코드 정비 화면을 넣지 않습니다.
- 빨간 글자 차이 찾기 도구처럼 문자 단위 diff만 보여 주지 않습니다.
- 문장 블록을 문법 순서대로 배열하는 활동보다 의미 조각의 포함·범위·확실성을 검사합니다.
- 최종 산출물은 점수표나 기사·보고서가 아니라 단계별 변화와 복구 근거를 담은 `전달 보존 기록`입니다.

## 6. MVP 제품 계약

### 기본 정보

| 항목 | 결정 |
|---|---|
| 앱 이름 | 정보 손실 통신소 |
| 학생용 부제 | 전해지는 동안 달라진 뜻을 찾아 안전하게 다시 보내요 |
| 주 대상 | 초등 3~6학년, 개인 또는 2인 모둠 |
| 기본 항로 | 3~4학년용 안내 1개 + 고정 사건 5개 |
| 확장 항로 | 5~6학년용 설명 카드 + 고정 사건 5개 |
| 1회 활동 시간 | 항로 하나당 15~20분 |
| 자료 유형 | 교실 안내, 모둠 준비 메모, 도서관 공지, 방과 후 일정, 가상 방송 원고 |
| 전달 단계 | 3~4학년 2단계, 5~6학년 3단계 |
| 핵심 변화 | 생략, 근거 없는 추가, 의미 변경, 의미 보존 바꾸어 쓰기 |
| 핵심 조작 | 의미 조각 확인, 표현 조각 선택, 변화 유형 선택, 필수 정보 선택, 전달문 블록 구성 |
| 드래그 | 필수 조작으로 사용하지 않음 |
| 입력 | 버튼·체크박스·라디오 그룹·문장 블록만 사용, 자유 입력 없음 |
| 저장 | React 메모리만 사용하며 새로고침 시 초기화 |
| 네트워크 | 초기 정적 자원 외 요청 없음 |
| 개인정보 | 이름·학번·학급·실제 대화·사진·음성·연락처·자유 입력 수집 없음 |
| 평가 | 점수 없이 변화 발견·분류·근거·필수 정보 보존 완료 여부만 요약 |
| 최종 산출물 | 원문 의미 조각, 단계별 변화, 처음 변한 지점, 복구한 정보, 안전 전달문을 담은 `전달 보존 기록` |

### 학생 학습 목표

학생은 활동 후 다음을 할 수 있어야 합니다.

1. 원문에서 전달 목적에 꼭 필요한 중심 정보와 보충 정보를 구분합니다.
2. 원문과 1차 전달문, 1차 전달문과 재전달문을 차례로 비교합니다.
3. 이전 단계에 있었지만 다음 단계에서 빠진 의미를 찾습니다.
4. 이전 단계에 없는데 새로 생긴 추측·평가·세부 정보를 찾습니다.
5. 시간·장소·수량·조건·부정·확실성이 달라진 표현을 찾습니다.
6. 표현은 달라도 뜻이 같은 바꾸어 쓰기를 오류와 구분합니다.
7. 정보가 처음 달라진 전달 단계를 근거와 함께 설명합니다.
8. 전달 대상과 매체에 필요한 의미 조각을 골라 안전한 전달문을 구성합니다.
9. 여러 문장이 서로 달라도 같은 필수 의미를 보존할 수 있음을 이해합니다.
10. 실제 사람에 대한 소문이나 확인되지 않은 내용을 전달하지 않는 태도를 기릅니다.

### MVP에서 제외

- 실제 학생 대화, 학급 단체 채팅, 문자, 이메일, SNS 게시물 가져오기
- 자유 문장 입력, 맞춤법 검사, 문장 유사도 계산, 자동 요약, 자연어 의미 분석
- 생성형 AI 문장 생성·채점·설명과 외부 언어 모델 호출
- 음성 녹음·인식·합성, 귓속말 게임, 학생끼리 실제 메시지 전송
- 특정 학생·교사·가족을 소재로 한 소문, 갈등, 괴롭힘, 상담, 건강·가정 정보
- 실제 재난·의료·법률 안내의 적절성 판정과 긴급 상황 훈련 대체
- 외부 주장의 사실성, 뉴스 출처 신뢰도, 증거 타당성 평가
- 데이터 표의 누락·중복·라벨·형식·대표성 검사
- 문장 성분, 품사, 맞춤법, 띄어쓰기, 문장 부호, 어순 맞히기
- 무작위 사건, 제한 시간, 점수, 생명, 순위, 배지, 콤보
- 학생 계정, 학급 코드, 교사 대시보드, 분석 로그, 클라우드 저장
- 인쇄·PDF 다운로드, 답 공유 게시판, 실제 메신저 전송
- GitHub 저장소 생성, 배포, HVC 등록, 정적 갤러리 동기화

## 7. 결정적 의미 데이터 모형

### 핵심 타입 초안

```ts
type GradeRoute = 'grade-3-4' | 'grade-5-6';

type MeaningKind =
  | 'actor'
  | 'action'
  | 'time'
  | 'place'
  | 'quantity'
  | 'condition'
  | 'negation'
  | 'certainty'
  | 'source'
  | 'detail';

type ChangeType =
  | 'omission'
  | 'unsupported-addition'
  | 'meaning-shift'
  | 'meaning-preserving'
  | 'unchanged';

interface MeaningUnit {
  id: string;
  kind: MeaningKind;
  canonicalMeaning: string;
  studentLabel: string;
  requiredForPurpose: boolean;
  allowedParaphraseIds: string[];
}

interface PhraseSegment {
  id: string;
  text: string;
  meaningUnitIds: string[];
  introducesUnsupportedMeaningIds: string[];
  accessibilityLabel: string;
}

interface TransmissionStage {
  id: string;
  order: number;
  senderRole: string;
  audienceRole: string;
  medium: 'spoken' | 'memo' | 'notice' | 'broadcast';
  segments: PhraseSegment[];
  expressedMeaningUnitIds: string[];
}

interface StageChange {
  id: string;
  fromStageId: string;
  toStageId: string;
  sourceSegmentIds: string[];
  targetSegmentIds: string[];
  meaningUnitIds: string[];
  type: ChangeType;
  explanation: string;
}

interface RelayOption {
  id: string;
  text: string;
  meaningUnitIds: string[];
  unsupportedMeaningIds: string[];
  validForAudienceIds: string[];
}

interface TransmissionCase {
  id: string;
  route: GradeRoute;
  title: string;
  purpose: string;
  audienceId: string;
  meaningUnits: MeaningUnit[];
  stages: TransmissionStage[];
  expectedChanges: StageChange[];
  relayOptions: RelayOption[];
  requiredMeaningUnitIds: string[];
  teacherNotes: string[];
}
```

### 데이터 불변 조건

- 각 사건은 2개 이상의 전달 단계를 가지며 `order`는 0부터 중복 없이 이어져야 합니다.
- 첫 단계는 원문이며 모든 `requiredMeaningUnitIds`를 표현해야 합니다.
- 모든 `MeaningUnit`, `PhraseSegment`, `TransmissionStage`, `StageChange`, `RelayOption` ID는 사건 안에서 고유해야 합니다.
- 각 단계의 `expressedMeaningUnitIds`는 해당 단계 조각의 의미 ID 합집합과 일치해야 합니다.
- 각 변화는 반드시 서로 인접한 단계만 연결해야 합니다.
- `omission`의 의미 ID는 이전 단계에 있고 다음 단계에는 없어야 합니다.
- `unsupported-addition`의 의미 ID는 이전 단계와 원문의 허용 의미 집합에 없어야 합니다.
- `meaning-shift`는 이전·다음 표현이 연결되지만 최소 하나의 의미 속성 또는 범위가 달라야 합니다.
- `meaning-preserving`은 표현 문자열이 달라도 의미 ID 집합과 필수 범위가 같아야 합니다.
- 모든 표현 조각은 화면에서 선택 가능한 완전한 문자열과 별도의 접근성 이름을 가져야 합니다.
- 모든 사건에는 최소 1개의 유효한 안전 전달문이 있어야 하며 복수 답이 가능한 사건을 최소 2개 둡니다.
- 유효 전달문은 필수 의미 ID를 모두 포함하고 근거 없는 의미 ID를 하나도 포함하지 않아야 합니다.
- 조건·부정·수량·확실성 의미를 필수로 둔 사건은 재작성 답에도 같은 범위를 보존해야 합니다.
- 콘텐츠 오류는 학생 오답으로 처리하지 않고 사건 시작을 막은 뒤 교사용 진단 ID를 표시합니다.

## 8. 핵심 학습 순환과 판정

### 사건별 7단계

1. **통신 임무 확인:** 누가 누구에게 어떤 목적으로 메시지를 전달하는지 확인합니다.
2. **원문 뜻 펼치기:** 원문의 의미 조각을 `누가·무엇·언제·어디서·조건·확실성` 카드로 확인합니다.
3. **인접 단계 비교:** 한 화면에서 이전 전달문과 다음 전달문만 나란히 읽습니다.
4. **변화 위치 표시:** 다음 단계에서 달라졌다고 생각한 표현 조각을 버튼으로 선택합니다.
5. **변화 유형 판정:** `빠짐·새로 더함·뜻이 달라짐·뜻을 지킨 바꾸어 쓰기` 중 하나와 근거 의미 조각을 고릅니다.
6. **전체 사슬 복구:** 모든 전달 단계를 펼쳐 정보가 처음 변한 지점과 누적된 결과를 확인합니다.
7. **안전한 전달문 구성:** 전달 목적에 필수인 의미 조각을 선택하고 검수된 표현 블록으로 문장을 만든 뒤 보존 기록을 확인합니다.

### 학생 답 판정 순서

1. 비교 중인 단계 쌍이 현재 사건의 인접 단계인지 확인합니다.
2. 학생이 고른 표현 조각 ID 집합을 중복 제거합니다.
3. 해당 단계 쌍의 검수된 변화 중 선택 조각과 연결되는 후보를 찾습니다.
4. 학생이 고른 변화 유형과 후보 변화의 유형을 비교합니다.
5. 학생이 선택한 근거 의미 ID가 후보의 의미 ID 집합과 연결되는지 확인합니다.
6. 변화 유형은 맞지만 조각이 일부 빠졌으면 한 번에 한 항목만 보완 안내합니다.
7. 검수된 복수 표현 조합이 있으면 어느 조합이든 타당한 답으로 인정합니다.

### 안전 전달문 검증 순서

1. 선택한 전달문 블록의 의미 ID 합집합을 계산합니다.
2. 사건의 필수 의미 ID가 모두 포함되었는지 확인합니다.
3. 선택 블록에 근거 없는 의미 ID가 포함되었는지 확인합니다.
4. 조건·부정·수량·확실성의 범위가 원문과 같은지 확인합니다.
5. 대상과 매체에 허용된 표현인지 확인합니다.
6. 문장 순서가 달라도 위 조건을 충족하면 타당한 전달문으로 인정합니다.
7. 맞춤법·띄어쓰기·문장 길이·원문 문자열 일치 정도로 점수를 주지 않습니다.

### 피드백 원칙

| 상황 | 피드백 예시 | 다음 행동 |
|---|---|---|
| 변화 위치를 못 찾음 | `다음 문장의 시간 표현을 이전 문장과 나란히 살펴봐요.` | 해당 의미 종류만 강조 |
| 생략과 의미 변경 혼동 | `시간 말이 완전히 사라졌나요, 아니면 다른 시간으로 남았나요?` | 두 정의 카드 비교 |
| 바꾸어 쓰기를 오류로 선택 | `말은 달라도 모이는 장소와 행동은 그대로예요.` | 대응 의미 조각 연결 |
| 근거 없는 추가를 세부 설명으로 판단 | `“새것을 사 오기”는 이전 단계 어디에서 확인할 수 있나요?` | 이전 단계에서 근거 찾기 |
| 필수 조건 누락 | `비가 올 때만 장소가 바뀐다는 조건도 함께 전해야 해요.` | 조건 블록 후보 표시 |
| 불확실성을 확정으로 변경 | `“예정”과 “확정”은 확실한 정도가 달라요.` | 확실성 눈금 표시 |
| 타당한 복수 재작성 | `표현은 다르지만 꼭 필요한 뜻을 모두 지켰어요.` | 다른 답과 의미 조각 비교 |

### 채점하지 않을 것

- 원문과 같은 낱말을 사용한 개수
- 가장 짧은 문장을 만든 학생
- 표현 블록을 배열한 유일한 순서
- 띄어쓰기와 문장 부호의 사소한 차이
- 사건 완료 속도와 다시 시도한 횟수
- 실제 사람의 말하기 능력, 기억력, 의도, 책임
- 학생 사이의 전달 정확도 순위

## 9. 고정 사건 계획

### 안내 활동: 세 문장이 같은 뜻일까요

- 원문: `각 모둠은 색종이 한 묶음을 준비해요.`
- 전달 A: `모둠별로 색종이 한 묶음을 준비해요.`
- 전달 B: `각자 색종이 한 묶음을 준비해요.`
- 학생은 A가 뜻을 지킨 바꾸어 쓰기이고 B는 준비 주체의 범위가 달라졌음을 비교합니다.
- 튜토리얼에서는 글자 차이보다 `각 모둠`과 `각자`의 의미 조각을 먼저 보게 합니다.

### 사건 1: 비 오는 날 모임 장소

- 대상: 3~4학년 기본
- 원문 핵심: `금요일 2시`, `운동장`, `비가 오면 체육관`, `모둠 안내판 앞`
- 1차 전달에서 `모둠 안내판 앞`이 빠지고, 재전달에서 `비가 오면` 조건이 빠집니다.
- 학생은 보충 정보의 생략과 필수 조건의 생략이 전달 목적에 미치는 차이를 봅니다.
- 안전 전달문은 시간·기본 장소·우천 조건을 모두 보존해야 하며 위치 세부 정보는 대상에 따라 선택할 수 있습니다.

### 사건 2: 도서관 반납 안내

- 대상: 3~4학년 기본
- 원문 핵심: `빌린 책`, `수요일까지`, `도서관 반납함`
- 1차 전달은 뜻을 유지한 바꾸어 쓰기를 사용합니다.
- 재전달에는 원문에 없는 `늦으면 대출할 수 없음`이라는 불이익이 추가됩니다.
- 학생은 더 구체적이고 그럴듯해 보여도 근거가 없으면 추가 정보임을 판정합니다.

### 사건 3: 만들기 재료 수량

- 대상: 3~6학년 공통
- 원문 핵심: `모둠별`, `종이컵 두 개까지`, `사용 가능`
- 전달 과정에서 `두 개까지`가 `두 개`로 바뀌고, 이후 `학생마다 두 개`로 바뀝니다.
- 학생은 낱말이 일부 남아 있어도 최대 수량과 배분 단위가 달라지면 의미 변경임을 확인합니다.
- 안전 전달문은 `모둠별`과 `최대 두 개`의 범위를 함께 보존해야 합니다.

### 사건 4: 방과 후 일정은 아직 예정

- 대상: 5~6학년 확장
- 원문 핵심: `다음 주`, `화요일로 바뀔 예정`, `월요일에 최종 확인`, `담당 선생님 안내`
- 1차 전달에서 출처가 빠지고, 재전달에서 `예정`이 `확정`으로 바뀝니다.
- 학생은 출처 생략과 확실성 변경을 별개로 표시합니다.
- 안전 전달문은 변경 가능성과 확인 시점을 보존하며, 확정된 일정처럼 쓰지 않아야 합니다.

### 사건 5: 가상 학교 방송 인수인계

- 대상: 5~6학년 종합
- 원문은 6개의 의미 조각과 원문→메모→방송 초안의 3단계를 가집니다.
- 한 단계에는 뜻을 지킨 바꾸어 쓰기, 다음 단계에는 조건 생략·시간 변경·근거 없는 준비물 추가가 함께 나타납니다.
- 학생은 각 변화가 처음 생긴 단계를 표시하고 최종문에서 누적된 손실을 원문과 대조합니다.
- 복구 활동에는 서로 다른 문장 구조의 유효 답을 최소 2개 제공합니다.
- 결과 기록은 `처음 변한 단계`, `누적된 변화`, `복구한 필수 의미`, `남기지 않은 추측`을 보여 줍니다.

### 사건 배열 원칙

- 첫 사건은 한 단계에 한 변화만 두고 두 번째 사건부터 의미 보존과 오류를 함께 배치합니다.
- 3~4학년 사건은 문장당 25어절 이하, 의미 조각 3~4개, 전달 단계 2개를 기본으로 합니다.
- 5~6학년 사건은 의미 조각 5~7개, 전달 단계 3개, 조건·확실성·출처 중 1개 이상을 포함합니다.
- 실제 학교명·학생명·연락처를 쓰지 않고 중립적인 역할명과 가상 장소만 사용합니다.
- 벌, 불이익, 사고, 질병, 괴롭힘을 자극적으로 사용하지 않습니다.
- 복수의 의미 보존 표현은 국어 교사 또는 초등 교사의 문장 검수를 거칩니다.

## 10. 상태 모델과 화면 전이

```ts
type LearningPhase =
  | 'welcome'
  | 'route-select'
  | 'tutorial'
  | 'mission-brief'
  | 'meaning-unpack'
  | 'stage-compare'
  | 'change-classify'
  | 'chain-review'
  | 'safe-relay'
  | 'case-result'
  | 'final-report';

interface LearnerState {
  phase: LearningPhase;
  route: GradeRoute | null;
  caseIndex: number;
  transitionIndex: number;
  selectedSegmentIds: string[];
  selectedChangeType: ChangeType | null;
  selectedEvidenceMeaningIds: string[];
  checkedChangeIds: string[];
  selectedRequiredMeaningIds: string[];
  selectedRelayOptionIds: string[];
  feedback: FeedbackState | null;
  completedCaseIds: string[];
  reportEntries: RelayReportEntry[];
}
```

### 상태 불변 조건

- 항로를 고르기 전에는 사건을 시작할 수 없습니다.
- 원문 의미 펼치기를 마치기 전에는 전달문 변화 판정을 열지 않습니다.
- 한 번에 인접한 두 단계만 비교하며 완료 후 다음 단계 쌍으로 이동합니다.
- 표현 조각을 하나 이상 선택해야 변화 유형을 제출할 수 있습니다.
- 판정 후에도 이전 선택을 수정할 수 있고 시도 횟수는 저장하지 않습니다.
- 모든 단계 쌍을 확인해야 전체 사슬 점검으로 이동합니다.
- 전체 사슬에서 필수 의미를 확인해야 안전 전달문 구성을 시작합니다.
- 사건 결과에는 학생의 선택과 검수 답을 구분해 표시합니다.
- 최종 보고서는 완료한 사건만 포함하며 미완료 사건을 실패로 표시하지 않습니다.
- 새로고침하면 초기 상태로 돌아오며 이전 학생의 선택이 남지 않습니다.

## 11. 화면 구성과 상호작용

### 공통 화면 구조

| 영역 | 내용 | 접근성 대안 |
|---|---|---|
| 상단 통신판 | 앱 이름, 현재 항로, 사건 번호, 업데이트 내역 | 제목·진행 상태를 텍스트로 제공 |
| 임무 브리핑 | 전달 목적, 보내는 역할, 받는 역할, 매체 | 아이콘만 쓰지 않고 모든 역할을 문장으로 설명 |
| 전달 이력선 | 원문·1차 전달·재전달 단계와 현재 비교 구간 | 순서 있는 목록과 `현재 1단계와 2단계 비교` 상태 제공 |
| 메시지 비교대 | 이전 메시지와 다음 메시지의 표현 조각 | 각 조각을 버튼으로 제공하고 읽기 순서 고정 |
| 의미 장부 | 의미 종류, 원문 정보, 현재 보존 여부 | 표 형태 텍스트 대안과 상태 문구 제공 |
| 판정 패널 | 변화 유형, 근거 의미 조각, 제출·수정 | 라디오 그룹과 체크박스 사용 |
| 도움 패널 | 정의 다시 보기, 한 변화만 보기, 글자 크게 | 토글 상태와 단축키 안내 |
| 하단 조작대 | 이전, 다시 확인, 다음 | 최소 44×44px, 명확한 비활성 이유 제공 |

### 첫 실행 안내

- 1단계: `글자가 달라도 뜻이 같을 수 있어요.`를 예시 두 문장으로 확인합니다.
- 2단계: `말이 남아 있어도 수량·조건이 달라지면 뜻이 바뀌어요.`를 범위 카드로 확인합니다.
- 3단계: 원문과 다음 전달문을 나란히 읽고 한 표현 조각을 버튼으로 고릅니다.
- 4단계: 변화 유형과 근거를 고른 뒤 의미 장부가 어떻게 달라지는지 확인합니다.
- 안내는 건너뛸 수 있고 사건 중 `비교 방법 다시 보기`로 재진입할 수 있습니다.

### 드래그 없는 조작

- 표현 조각은 클릭 가능한 문장 구간 버튼이며 선택 순서는 판정에 영향을 주지 않습니다.
- 전달문 구성은 블록 선택 후 `앞으로`, `뒤로`, `빼기` 버튼으로 조정할 수 있습니다.
- 순서가 의미에 영향을 주지 않는 답은 블록 배열 순서를 채점하지 않습니다.
- 키보드에서는 Tab으로 이동하고 Space 또는 Enter로 선택합니다.
- 전달 단계 전환은 `이전 단계`, `다음 단계` 버튼과 단계 탭을 모두 제공합니다.
- 좁은 화면에서는 두 메시지를 위아래로 배치하되 `이전 메시지`, `다음 메시지` 표지를 고정합니다.

### 결과 화면

- `뜻을 지켜 전달한 정보`, `처음 빠진 정보`, `근거 없이 더해진 정보`, `뜻이 달라진 표현`을 별도 묶음으로 보여 줍니다.
- 변화마다 처음 발생한 단계와 이후 단계에 남았는지를 표시합니다.
- 학생이 만든 전달문과 다른 유효 예시를 나란히 보여 주되 우열 점수를 매기지 않습니다.
- `원문 그대로 복사`가 아니라 필요한 정보를 간결하게 보존했다는 점을 의미 조각 체크로 보여 줍니다.
- 실제 친구 이야기를 전달하지 않는다는 안전 문구와 교실 토론 질문을 함께 제공합니다.

## 12. 시각·언어 디자인

### 시각 콘셉트

- 화면을 사람 캐릭터가 소문을 퍼뜨리는 장면이 아니라 중립적인 통신 기록실과 의미 장부로 표현합니다.
- 각 전달 단계는 `원문`, `1차 전달`, `재전달` 번호와 역할명을 함께 표시합니다.
- 변화 유형은 색뿐 아니라 아이콘과 문구를 함께 사용합니다.
- `빠짐`은 빈 윤곽, `추가`는 더하기 표지, `의미 변경`은 양방향 화살표, `뜻 유지`는 연결 고리로 표현합니다.
- 빨간색은 전체 오답 표시가 아니라 검토가 필요한 변화에만 제한적으로 사용합니다.
- 원문과 전달문을 글자 단위로 겹치지 않고 의미 조각 단위로 묶어 읽기 부담을 줄입니다.

### 학생용 문장 원칙

- `손실`, `왜곡`, `명제`, `스코프` 같은 말은 첫 화면에서 사용하지 않습니다.
- 학생 화면에서는 `빠졌어요`, `새로 더했어요`, `뜻이 달라졌어요`, `뜻을 지켰어요`를 사용합니다.
- 교사용 설명과 결과 기록에서만 `정보 손실`, `근거 없는 추가`, `의미 변경`, `의미 보존`을 병기합니다.
- 피드백은 한 번에 한 변화와 확인할 위치를 알려 주고 정답 문장을 즉시 공개하지 않습니다.
- `틀렸어요` 대신 `어느 단계에서 처음 달라졌는지 다시 살펴봐요`처럼 재검토 행동을 안내합니다.
- 실제 말실수의 책임을 묻는 표현, 기억력이 나쁘다는 표현, 소문꾼 캐릭터를 사용하지 않습니다.

### 글자와 조작 크기

- 기본 본문은 모바일 17px 이상, 데스크톱 18px 이상을 권장합니다.
- 메시지 본문은 1.6 이상의 줄간격과 문단당 최대 3문장을 사용합니다.
- 의미 조각 버튼은 최소 높이 48px, 주요 이동 버튼은 최소 52px로 설계합니다.
- 문장 한 줄 길이는 넓은 화면에서도 약 65자로 제한합니다.
- 긴 복합문은 의미 조각 경계에 맞추어 시각적 여백을 둡니다.

## 13. 접근성 및 반응형 계획

### 키보드와 보조 기술

- 모든 기능을 포인터 없이 사용할 수 있어야 합니다.
- 표현 조각은 실제 `button`, 변화 유형은 `fieldset`과 라디오 입력, 의미 선택은 체크박스를 사용합니다.
- 선택 상태는 `aria-pressed`, 현재 단계는 `aria-current="step"`로 알립니다.
- 메시지 비교 영역에는 `이전 전달문`, `다음 전달문` 제목을 제공하고 DOM 읽기 순서를 유지합니다.
- 의미 장부는 시각 카드와 동일한 정보를 표 또는 정의 목록으로 제공합니다.
- 피드백은 `aria-live="polite"`로 읽고 긴 전체 문장을 매번 반복하지 않습니다.
- 오류 발생 시 초점을 첫 보완 항목으로 이동하고 색만으로 상태를 전달하지 않습니다.
- 애니메이션 없이도 단계 변화와 의미 조각 연결을 이해할 수 있어야 합니다.

### 반응형 화면

- 360px 이상 휴대전화에서 가로 스크롤 없이 핵심 활동을 완료합니다.
- 360~599px에서는 두 메시지와 의미 장부를 세로로 배치하고 하단 조작대를 고정합니다.
- 600~959px에서는 메시지를 위아래로 두고 의미 장부를 접을 수 있는 패널로 제공합니다.
- 960px 이상에서는 이전·다음 메시지를 2열로, 의미 장부를 오른쪽 보조 열로 배치합니다.
- 단계 이력선은 좁은 화면에서 번호 목록으로 바뀌며 가로 스와이프를 필수로 요구하지 않습니다.
- 200% 확대에서도 문장 조각, 판정 버튼, 피드백이 겹치지 않아야 합니다.

### 색과 움직임

- 변화 유형은 색·아이콘·텍스트 세 가지로 중복 표시합니다.
- 텍스트 대비는 WCAG AA를 목표로 합니다.
- 연결 애니메이션은 200ms 안팎으로 제한하고 `prefers-reduced-motion`에서 제거합니다.
- 깜빡임, 자동 재생, 제한 시간 표시, 긴 축하 애니메이션을 사용하지 않습니다.
- 선택 전후 레이아웃이 크게 움직이지 않도록 피드백 공간을 미리 확보합니다.

## 14. 개인정보와 관계 안전 원칙

- 앱은 검수된 가상 메시지만 제공하고 학생이 실제 메시지를 입력·업로드하지 못하게 합니다.
- 이름, 학번, 학급, 연락처, 위치, 사진, 음성, 클립보드 내용을 수집하지 않습니다.
- 실제 학생의 실수나 교실 소문을 재연하는 과제를 제안하지 않습니다.
- 사건 인물은 `방송 담당`, `도서관 안내자`, `모둠 대표` 같은 중립적인 역할명으로 표시합니다.
- 괴롭힘·따돌림·가족사·질병·경제 상황·성적·징계·사고를 사건 소재로 사용하지 않습니다.
- 실제 긴급 안내를 앱 결과에 의존해 다시 쓰게 하지 않으며 앱이 재난·의료 훈련을 대체하지 않는다고 밝힙니다.
- `안전한 전달문`은 학습 사건의 필수 의미 보존을 뜻하며 현실의 모든 위험을 보증한다는 의미로 사용하지 않습니다.
- 외부 분석, 광고, 추적 스크립트, 쿠키, 서비스 워커 영구 캐시를 사용하지 않습니다.
- 새로고침과 탭 닫기 시 학생의 선택은 사라집니다.

## 15. 권장 프로젝트 구조

```text
information-loss-communication-station/
├── docs/
│   ├── curriculum-alignment.md
│   ├── meaning-change-rubric.md
│   ├── content-review-checklist.md
│   ├── relationship-safety-checklist.md
│   ├── accessibility-checklist.md
│   ├── progress.md
│   └── test-log.md
├── e2e/
│   ├── grade-3-4-flow.spec.ts
│   ├── grade-5-6-flow.spec.ts
│   ├── keyboard-flow.spec.ts
│   ├── responsive.spec.ts
│   └── privacy.spec.ts
├── src/
│   ├── app/
│   │   ├── App.tsx
│   │   ├── app-reducer.ts
│   │   ├── app-state.ts
│   │   └── routes.ts
│   ├── components/
│   │   ├── TransmissionTimeline.tsx
│   │   ├── MessageComparison.tsx
│   │   ├── PhraseSegmentButton.tsx
│   │   ├── MeaningLedger.tsx
│   │   ├── ChangeTypePanel.tsx
│   │   ├── SafeRelayBuilder.tsx
│   │   ├── FeedbackPanel.tsx
│   │   ├── UpdateHistoryDialog.tsx
│   │   └── TeacherGuideDialog.tsx
│   ├── content/
│   │   ├── cases-grade-3-4.ts
│   │   ├── cases-grade-5-6.ts
│   │   ├── tutorial-case.ts
│   │   ├── change-glossary.ts
│   │   └── update-history.ts
│   ├── domain/
│   │   ├── types.ts
│   │   ├── meaning-ledger.ts
│   │   ├── change-judge.ts
│   │   ├── safe-relay-validator.ts
│   │   ├── content-validator.ts
│   │   └── report-builder.ts
│   ├── screens/
│   │   ├── WelcomeScreen.tsx
│   │   ├── RouteSelectScreen.tsx
│   │   ├── TutorialScreen.tsx
│   │   ├── MissionBriefScreen.tsx
│   │   ├── MeaningUnpackScreen.tsx
│   │   ├── StageCompareScreen.tsx
│   │   ├── ChainReviewScreen.tsx
│   │   ├── SafeRelayScreen.tsx
│   │   ├── CaseResultScreen.tsx
│   │   └── FinalReportScreen.tsx
│   ├── styles/
│   │   ├── tokens.css
│   │   ├── layout.css
│   │   ├── components.css
│   │   └── responsive.css
│   ├── test/
│   │   ├── fixtures.ts
│   │   └── setup.ts
│   └── main.tsx
├── index.html
├── package.json
├── playwright.config.ts
├── tsconfig.json
└── vite.config.ts
```

### 분리 원칙

- 각 코드 파일은 500줄 미만으로 유지합니다.
- 사건 원고와 판정 로직을 같은 파일에 두지 않습니다.
- 3~4학년과 5~6학년 사건 데이터를 별도 파일로 나눕니다.
- 의미 집합 계산, 변화 판정, 안전 전달문 검증은 React와 독립된 순수 함수로 작성합니다.
- 화면 컴포넌트는 판정 정답을 직접 포함하지 않고 도메인 함수 결과만 표시합니다.
- 업데이트 내역, 교사용 안내, 사건 콘텐츠는 코드 수정 없이 데이터 항목을 추가할 수 있게 분리합니다.
- 콘텐츠 파일이 커지면 사건 단위 파일과 인덱스로 다시 나눕니다.

## 16. 테스트 및 콘텐츠 검증 계획

### 도메인 단위 테스트

- 인접 단계만 변화 비교 대상으로 인정하는지 검사합니다.
- `omission` 의미가 이전 단계에는 있고 다음 단계에는 없는지 검사합니다.
- `unsupported-addition`이 원문·이전 단계에 없는 의미인지 검사합니다.
- 문자열이 달라도 같은 의미 ID 집합이면 `meaning-preserving`으로 인정하는지 검사합니다.
- 같은 낱말이 있어도 수량·조건·부정·확실성 범위가 다르면 `meaning-shift`인지 검사합니다.
- 학생 선택 집합의 순서와 중복이 판정에 영향을 주지 않는지 검사합니다.
- 필수 의미를 모두 포함한 복수의 전달문 조합을 인정하는지 검사합니다.
- 근거 없는 의미가 하나라도 포함되면 안전 전달문으로 통과하지 않는지 검사합니다.
- 미완료 사건과 콘텐츠 오류를 학생 오답으로 기록하지 않는지 검사합니다.

### 콘텐츠 검증

- 모든 참조 ID와 단계 순서가 유효한지 검사합니다.
- 원문의 필수 의미 ID가 원문 표현 조각과 모두 연결되는지 검사합니다.
- 각 변화 유형이 데이터 정의와 실제 단계 차이에 일치하는지 검사합니다.
- 같은 변화가 두 단계에 중복해 `처음 변한 지점`으로 등록되지 않았는지 검사합니다.
- 의미 보존 바꾸어 쓰기가 조건·부정·수량·확실성을 실제로 보존하는지 국어 교사가 검수합니다.
- 안전 전달문 선택지에 근거 없는 평가·추측·불이익이 없는지 검사합니다.
- 복수 정답 사건에 유효한 답이 최소 2개 있는지 검사합니다.
- 실제 학교·학생 식별 정보와 민감한 상황이 원고에 없는지 검사합니다.
- 3~4학년과 5~6학년의 문장 길이·의미 조각 수·단계 수 제한을 검사합니다.

### 컴포넌트 및 통합 테스트

- 항로 선택 후 올바른 사건 묶음이 열리는지 확인합니다.
- 의미 펼치기 전 판정 단계가 잠겨 있는지 확인합니다.
- 표현 조각 선택과 변화 유형 제출 후 적절한 피드백이 표시되는지 확인합니다.
- 답을 수정해도 시도 횟수나 실패 점수가 나타나지 않는지 확인합니다.
- 단계 이력선과 텍스트 목록의 현재 단계가 일치하는지 확인합니다.
- 안전 전달문 블록 순서를 바꾸어도 의미 집합이 같으면 통과하는지 확인합니다.
- 결과 화면의 학생 선택과 검수 답이 명확히 구분되는지 확인합니다.
- 새로고침 후 이전 학생 상태가 남지 않는지 확인합니다.
- 업데이트 내역과 교사용 안내를 키보드로 열고 닫을 수 있는지 확인합니다.

### 브라우저 E2E 점검

- 3~4학년 항로의 안내부터 최종 전달 보존 기록까지 완주합니다.
- 5~6학년 항로에서 3단계 메시지의 확실성 변경을 찾아 복구합니다.
- 뜻을 지킨 바꾸어 쓰기를 오류로 선택했을 때 재검토 피드백을 확인합니다.
- 필수 조건을 뺀 전달문이 통과하지 않고 조건 보완 안내를 제공하는지 확인합니다.
- 복수의 유효 전달문을 각각 완성해 동일하게 인정되는지 확인합니다.
- Tab, Shift+Tab, Enter, Space만으로 전체 흐름을 완주합니다.
- 360×800, 768×1024, 1280×800 화면에서 가로 넘침과 조작 가림이 없는지 확인합니다.
- 200% 확대와 `prefers-reduced-motion`에서 핵심 기능이 유지되는지 확인합니다.
- axe 검사에서 치명적·중요 접근성 위반이 없는지 확인합니다.
- 네트워크 기록에 정적 자원 이외 요청이 없는지 확인합니다.
- localStorage, sessionStorage, IndexedDB, 쿠키에 학생 선택이 기록되지 않는지 확인합니다.
- 실제 메시지 입력·파일 업로드·클립보드 읽기 기능이 없는지 확인합니다.

### 향후 실행할 품질 명령

아래 명령은 구현 단계에서 프로젝트 스크립트에 맞추어 확정합니다. 현재 계획 단계에서는 실행하지 않습니다.

```bash
npm run lint
npm run typecheck
npm run test
npm run build
npm run test:e2e
```

## 17. 교사용 수업 운영 계획

### 권장 수업 흐름

1. 교사는 서로 다른 두 문장이 같은 뜻일 수 있는지 짧은 예시로 묻습니다.
2. 학생은 앱에서 원문의 의미 조각을 확인하고 인접 전달 단계를 비교합니다.
3. 2인 모둠은 `빠짐`과 `뜻 변경`, `새 정보`와 `자세한 설명`의 차이를 말로 설명합니다.
4. 학생은 안전 전달문을 구성하고 다른 유효 문장과 공통으로 보존한 뜻을 찾습니다.
5. 교사는 실제 친구 이야기를 사용하지 않고 교과서 안내문이나 교사가 만든 가상 문장으로 후속 활동을 진행합니다.
6. 수업 마지막에는 `어떤 정보를 먼저 확인해야 정확히 전달할 수 있는가`를 돌아봅니다.

### 교사용 관찰 기록

| 관찰 항목 | 확인 질문 | 앱이 제공하는 근거 | 별도 관찰이 필요한 부분 |
|---|---|---|---|
| 중심 정보 | 전달 목적에 꼭 필요한 뜻을 골랐는가 | 필수 의미 선택과 복구 기록 | 실제 듣기에서 중심 내용을 찾는 과정 |
| 단계 비교 | 바로 앞 단계와 비교했는가 | 완료한 단계 쌍과 선택 조각 | 모둠 대화에서 비교 근거를 말하는 과정 |
| 변화 분류 | 생략·추가·변경·의미 보존을 구분하는가 | 사건별 판정과 근거 | 새로운 문장에 개념을 전이하는 능력 |
| 범위 보존 | 조건·부정·수량·확실성을 지켰는가 | 안전 전달문 의미 장부 | 자유 글쓰기에서 자연스럽게 표현하는 능력 |
| 관계 안전 | 실제 사람의 소문을 만들지 않는 이유를 이해하는가 | 안전 확인 카드 | 생활 속 의사소통 태도와 실천 |

### 교사용 고정 안내

- 앱은 실제 학생의 기억력이나 말하기 정확도를 비교하는 평가 도구가 아닙니다.
- 학생 이름을 넣어 소문 전달 게임으로 바꾸지 않습니다.
- 의미 보존 여부는 사건 데이터의 검수 범위 안에서만 판정됩니다.
- 자유로운 바꾸어 쓰기에는 앱에 없는 타당한 답이 있을 수 있으므로 교사가 추가 답을 인정할 수 있습니다.
- 실제 긴급 안내는 학교의 공식 전달 절차와 원문 확인을 따르며 앱 활동으로 대신하지 않습니다.
- 학생이 읽기나 언어 표현에 어려움이 있을 때는 조각 수를 줄이고 한 단계씩 공개합니다.

## 18. 업데이트 내역과 문서화

### 앱 내부 업데이트 내역

- 실제 구현 시 모든 화면에서 접근 가능한 작은 `업데이트 내역` 버튼을 둡니다.
- 대화상자에는 버전, 날짜, 학생에게 보이는 변화, 콘텐츠 검수 변경을 최신순으로 기록합니다.
- 최초 항목 예시는 `2026-07-17 · v0.1.0 · 최초 MVP 계획 수립`으로 시작하되 실제 개발 시작일과 버전은 구현 시 확정합니다.
- 사건 문장, 의미 주석, 정답 집합, 안전 안내를 바꾸면 코드 수정이 작아도 내역에 남깁니다.
- 개발 중이거나 배포되지 않은 내용을 완료된 업데이트처럼 표시하지 않습니다.

### 저장소 문서

- `README.md`: 학습 목표, 실행 방법, 비수집 원칙, 기능 범위
- `docs/curriculum-alignment.md`: 성취기준 직접·부분 연결과 범위 한계
- `docs/meaning-change-rubric.md`: 네 가지 핵심 변화의 정의와 경계 사례
- `docs/content-review-checklist.md`: 사건별 의미·문장·복수 답 검수 기록
- `docs/relationship-safety-checklist.md`: 가상 인물, 민감 소재, 소문 방지 검수
- `docs/accessibility-checklist.md`: 키보드, 화면 읽기, 확대, 색상 점검
- `docs/progress.md`: 승인된 계획, 구현 단계, 계획 변경과 이유
- `docs/test-log.md`: 명령, 날짜, 결과, 실패와 재검증 기록

## 19. 향후 구현 순서

1. 별도 구현 승인을 받은 뒤 계획 문서와 승인 범위를 새 저장소의 `docs/`에 복사합니다.
2. Vite·React·TypeScript 정적 프로젝트와 lint·typecheck·test·build 스크립트를 구성합니다.
3. 의미 단위, 전달 단계, 변화, 재작성 선택지 타입과 콘텐츠 검증기를 먼저 구현합니다.
4. 튜토리얼과 3~4학년 사건 1개를 작성하여 초등 국어 교사의 의미 주석 검수를 받습니다.
5. 의미 장부, 변화 판정, 안전 전달문 검증 순수 함수와 단위 테스트를 구현합니다.
6. 리듀서와 화면 전이를 구성하고 새로고침 초기화·점수 비노출 불변 조건을 고정합니다.
7. 메시지 비교대, 단계 이력선, 의미 장부, 판정 패널을 키보드 우선으로 구현합니다.
8. 안전 전달문 구성기와 복수 유효 답 처리를 구현합니다.
9. 3~4학년 5개 사건과 5~6학년 5개 사건을 완성하고 문장·관계 안전·교육과정 검수를 기록합니다.
10. 반응형, 화면 읽기, 축소 동작, 업데이트 내역, 교사용 안내를 완성합니다.
11. 단위·통합·E2E·접근성·개인정보 검사를 실행하고 `docs/test-log.md`에 결과를 기록합니다.
12. 승인된 계획과 구현 결과를 대조하여 누락·범위 확장·콘텐츠 위험을 리뷰합니다.
13. 사용자가 별도로 요청한 경우에만 커밋, GitHub 푸시, 배포, 실제 공개 주소 검증을 진행합니다.
14. HVC 관리자 등록과 공개 갤러리 동기화는 각각 별도 요청과 검증 절차로 진행합니다.

## 20. MVP 수용 기준

### 학습 기능

- 학생이 3~4학년 또는 5~6학년 항로를 선택할 수 있습니다.
- 사건마다 원문 의미 조각과 전달 목적을 먼저 확인합니다.
- 학생이 모든 인접 전달 단계 쌍을 차례로 비교할 수 있습니다.
- 생략·근거 없는 추가·의미 변경·의미 보존 바꾸어 쓰기를 각각 다룹니다.
- 정보가 처음 변한 단계와 최종 누적 결과를 함께 확인할 수 있습니다.
- 안전 전달문은 필수 의미를 보존하고 근거 없는 정보를 제외해야 통과합니다.
- 문자열이 다른 복수의 유효 전달문을 인정합니다.
- 최종 전달 보존 기록이 점수 없이 학생의 근거와 복구 내용을 요약합니다.

### 국어적 정확성과 콘텐츠 안전

- 글자 차이와 의미 차이를 같은 것으로 취급하지 않습니다.
- 짧게 줄였다는 이유만으로 좋은 요약으로 판정하지 않습니다.
- 조건·부정·수량·확실성의 범위 변경을 의미 변경으로 판정합니다.
- 원문이나 이전 단계에 없는 추측을 근거 없는 추가로 판정합니다.
- 자연스러운 바꾸어 쓰기와 복수의 유효 재작성안을 인정합니다.
- 모든 사건은 실제 학생과 무관한 가상 역할·장소를 사용합니다.
- 민감 정보, 소문, 괴롭힘, 실제 긴급 안내를 소재로 사용하지 않습니다.
- 국어 교사 또는 초등 교사의 콘텐츠 검수 결과가 기록되어 있습니다.

### 사용성 및 접근성

- 드래그 없이 모든 활동을 완료할 수 있습니다.
- 360px 화면과 200% 확대에서 가로 넘침 없이 사용할 수 있습니다.
- 모든 선택 상태와 변화 유형이 색 이외의 텍스트·아이콘으로 전달됩니다.
- 화면 읽기 프로그램에서 전달 단계, 메시지 순서, 선택 상태, 피드백을 이해할 수 있습니다.
- 움직임 줄이기 설정에서 애니메이션이 제거되어도 의미가 보존됩니다.
- 업데이트 내역과 교사용 안내를 모든 화면에서 열 수 있습니다.

### 개인정보 및 기술

- 실제 메시지, 이름, 사진, 음성, 연락처, 자유 입력을 수집하지 않습니다.
- 외부 API, 생성형 AI, 분석·광고·추적 요청이 없습니다.
- 학생 선택을 브라우저 영구 저장소에 기록하지 않습니다.
- 새로고침 후 이전 학생의 상태가 남지 않습니다.
- 모든 코드 파일이 500줄 미만이며 콘텐츠와 판정 로직이 분리되어 있습니다.
- lint, typecheck, unit, build, E2E, 접근성 검사가 통과합니다.

## 21. 주요 위험과 완화 방안

| 위험 | 영향 | 완화 계획 |
|---|---|---|
| 글자 차이를 의미 차이로 오해 | 동의 표현과 바꾸어 쓰기를 모두 오류로 학습 | 의미 ID와 검수된 바꾸어 쓰기를 데이터 중심에 두고 튜토리얼에서 먼저 비교 |
| 정답 문장 하나만 인정 | 자연스러운 학생 표현을 배제 | 문자열 대신 필수 의미 집합과 금지 의미 집합으로 복수 답 판정 |
| 요약을 무조건 짧게 만들기 | 조건·수량·확실성 같은 필수 정보 손실 | 전달 목적별 필수 의미를 먼저 고르고 길이는 채점하지 않음 |
| 팩트체크 편집국과 유사해짐 | 외부 사실 판정 앱처럼 보임 | 외부 자료·출처 등급·참거짓 판정을 제외하고 전달 이력만 비교 |
| AI 데이터 품질검사소와 유사해짐 | 누락·오류 분류 화면을 반복 | 데이터 표 대신 문장 단계·의미 조각·안전 재작성의 연속 학습 순환 사용 |
| 지시어 관제실과 유사해짐 | 문맥 속 대상 찾기 활동으로 축소 | 지시 대상보다 전달 단계 사이 전체 의미 보존과 최초 변화 지점을 중심화 |
| 소문 퍼뜨리기 놀이로 변질 | 관계 갈등·낙인·실수 조롱 가능 | 실제 학생 입력과 상호 전달을 제거하고 중립적 가상 안내만 사용 |
| 의미 주석 오류 | 타당한 바꾸어 쓰기를 오답 처리 | 사건별 국어 교사 이중 검수와 복수 답 회귀 테스트 유지 |
| 조건·확실성 개념이 어려움 | 저학년 인지 부담 증가 | 3~4학년은 시간·장소·수량부터 시작하고 5~6학년 확장에 조건·확실성 배치 |
| 긴 비교 화면 | 모바일 읽기 순서와 작업 기억 부담 | 인접 두 단계만 먼저 공개하고 전체 사슬은 판정 후 요약으로 제공 |
| 안전 전달문 표현의 과도한 의미 | 현실의 안전을 보장하는 도구로 오인 | 학습 사건의 의미 보존이라는 정의와 실제 긴급 안내 비대체 문구 고정 |

## 22. 구현 전 승인 게이트

구현을 시작하기 전에 다음 항목을 사용자가 검토·승인해야 합니다.

- 앱 이름, 학생용 부제, 권장 저장소 이름
- 3~4학년과 5~6학년을 한 앱의 두 항로로 제공하는 범위
- `생략·근거 없는 추가·의미 변경·의미 보존`의 네 가지 핵심 분류
- 실제 학생 입력과 귓속말 전달 활동을 제외하는 관계 안전 원칙
- 자유 서술 대신 검수된 표현 블록과 의미 집합으로 복수 답을 판정하는 방식
- 안내 1개와 학년군별 고정 사건 5개라는 콘텐츠 양
- 조건·부정·수량·확실성 범위 보존 규칙
- 서버·외부 API·생성형 AI·영구 저장을 사용하지 않는 정적 MVP 범위
- 업데이트 내역, 파일당 500줄 미만, 접근성·반응형·테스트 기준

승인 전에는 저장소 생성이나 구현을 시작하지 않습니다. 승인 뒤에도 커밋·푸시·배포·HVC 등록·공개 갤러리 동기화는 사용자의 요청 범위에 맞추어 각각 진행합니다.

## 23. 계획 완료의 정의

- 아이디어 출처와 기존 앱 비교 근거가 문서에 기록되어 있습니다.
- 3~6학년 국어 성취기준의 직접·부분 연결과 한계가 구분되어 있습니다.
- 글자 차이와 의미 차이를 구분하는 변화 유형 계약이 정의되어 있습니다.
- 전달 단계, 의미 단위, 변화, 복수 재작성안을 위한 데이터 모형이 정의되어 있습니다.
- 학년군별 학습 순환, 고정 사건, 피드백, 수용 기준이 구체화되어 있습니다.
- 팩트체크, 데이터 품질, 지시어, 문장 만들기 앱과의 차별화 경계가 명시되어 있습니다.
- 실제 학생 메시지를 사용하지 않는 개인정보·관계 안전 원칙이 포함되어 있습니다.
- 접근성, 반응형, 테스트, 업데이트 내역, 파일 분리 계획이 포함되어 있습니다.
- 구현 전에 사용자가 검토해야 할 승인 게이트가 명시되어 있습니다.
- 이 문서 작성 외의 구현·저장소·테스트·배포 작업은 수행하지 않았습니다.
